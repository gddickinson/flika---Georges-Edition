"""AI / LLM integration for flika.

All sub-modules use optional imports so that the core AI packages (anthropic,
cellpose, stardist, …) are **not** required unless the user actually invokes
an AI feature.
"""
